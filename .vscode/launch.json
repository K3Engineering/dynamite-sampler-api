{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Generate API",
            "type": "debugpy",
            "request": "launch",
            "module": "schema.generator",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}